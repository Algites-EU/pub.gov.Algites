package eu.algites.tool.build.credentials.coreintf;

/**
 * Canonical secret fields supported by Algites credential profiles.
 */
public enum AInCredentialField {
    USERNAME,
    PASSWORD,
    TOKEN,
    API_KEY,
    CERTIFICATE,
    PRIVATE_KEY,
    PRIVATE_KEY_PASSWORD
}
